# dbt — keep in lockstep with airflow/Dockerfile so package-lock.yml stays readable on both
# (dbt 1.9+ writes a `name:` key into the lock that older dbt can't parse → `dbt deps` fails).
dbt-core==1.11.11
dbt-postgres==1.8.2

# Airflow providers (install alongside apache-airflow in Docker)
apache-airflow-providers-airbyte==3.6.0
apache-airflow-providers-postgres==5.10.0

# Utilities
psycopg2-binary==2.9.9
python-dotenv==1.0.1
pandas==2.2.2
requests==2.31.0
pyyaml==6.0.1
